%p
  %b User:
  = @order.user.name
%p
  %b Total:
  = @order.price_dollars_with_symbol
%p
  %b Line Items:
- @order.line_items.each do |line_item|
  %p
    \#{line_item.item.name} x #{line_item.quantity} @ #{line_item.price_dollars_with_symbol}
    

= link_to 'Edit', edit_order_path(@order)
\|
= link_to 'Back', orders_path
